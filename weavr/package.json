{
  "name": "weavr-claw-agent",
  "private": true,
  "type": "module",
  "description": "The weavr layer of this Claw Agent fork: wallet tool, signer isolation, approval gate, host brief, tests.",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs' && python3 plugins/weavr-wallet-gate/test_gate.py",
    "test:patch": "python3 patches/test_trust_gate.py"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
